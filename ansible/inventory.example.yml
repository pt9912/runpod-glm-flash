all:
  hosts:
    glm:
      # Direct SSH address/port of the Pod (the mapped external port for 22/tcp is shown in the RunPod console).
      ansible_host: REPLACE_WITH_SSH_HOST
      ansible_port: 22 # replace with the mapped external port
      ansible_user: root
